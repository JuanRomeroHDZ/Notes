

para configurar el ospf directamente en las interfaces de red se emplea el comando " ip ospf " para esto se emplea la siguiente sintaxis en el cli.

```
Router(config-if)# ip ospf process-id area area-id
```

![[Pasted image 20260302225403.png]]